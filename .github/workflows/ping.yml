name: ping-render
on:
  schedule:
    - cron: "*/10 * * * *"   # каждые 10 минут
  workflow_dispatch:         # запуск вручную
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Curl ping
        run: |
          curl -fsS https://data-collector-gizw.onrender.com/ping || exit 1
